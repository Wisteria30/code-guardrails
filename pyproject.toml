[project]
name = "code-guardrails"
version = "0.1.0"
description = "Detect test doubles and unapproved fallbacks in production code"
requires-python = ">=3.12"
dependencies = [
    "PyYAML>=6.0",
]

[project.scripts]
check-policy = "check_policy:main"
